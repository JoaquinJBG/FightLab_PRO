# P0.1 — Banda de carga tipo Strava · Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sustituir en la vista de Carga el ACWR crudo (umbrales fijos 0.8–1.3, refutados por la auditoría v2) por una banda de rango personalizada con semáforo, derivada de la propia carga semanal reciente del atleta.

**Architecture:** Se añade `computeLoadBand` y un campo `band: LoadBand | null` al motor de carga cliente (`lib/load.ts`), calculado desde el `daily28` que `loadMetrics` ya produce. La vista `training/load/page.tsx` reemplaza la sección de ACWR por una tarjeta de banda; el ACWR se sigue calculando (para coach IA/backend) pero no se muestra. El backend no cambia en esta iteración: la UI preserva la banda local cuando las métricas de servidor no la traen.

**Tech Stack:** Next.js 16 (App Router, client components), React 19, TypeScript, Tailwind v4, Vitest + jsdom.

## Global Constraints

- Gestor de paquetes: **pnpm** (nunca npm). Tests: `pnpm test` (Vitest, ya configurado).
- Commits: mensaje en **castellano**, **sin** trailer `Co-Authored-By`. Formato Conventional Commits.
- Rama de trabajo: `feat/v2-vision-ampliada`.
- **Prohibido** introducir en la UI umbrales fijos de ACWR (0.8–1.3, >1.5, etc.): la auditoría v2 los refutó. Los límites de la banda salen SIEMPRE de los datos del propio atleta.
- El ACWR (`LoadMetrics.acwr`) se conserva en el motor; solo se retira de la vista de Carga.
- Alcance: **solo `lib/load.ts`, `lib/activities.ts` y `app/(shell)/training/load/page.tsx`**. Las tarjetas ACWR de `training/page.tsx`, `dashboard/page.tsx` y `coach/page.tsx` quedan FUERA de alcance (ver "Follow-up" al final).

---

## File Structure

- `frontend/lib/load.ts` — motor de carga. Se añaden los tipos `LoadBandStatus`/`LoadBand`, el campo `band` en `LoadMetrics`, y la función `computeLoadBand`.
- `frontend/lib/load.test.ts` — tests del motor. Se añaden casos para `band`.
- `frontend/lib/activities.ts` — `fetchServerMetrics` gana `band: null` (el servidor aún no lo provee).
- `frontend/app/(shell)/training/load/page.tsx` — la sección ACWR se sustituye por la tarjeta de banda.

---

## Task 1: Motor — `computeLoadBand` + campo `band` en `LoadMetrics`

**Files:**
- Modify: `frontend/lib/load.ts` (tipos tras `LoadPoint`; función nueva; return de `loadMetrics` línea 102; tipo `LoadMetrics` líneas 48–57)
- Modify: `frontend/lib/activities.ts:330-339` (añadir `band: null` al objeto devuelto)
- Test: `frontend/lib/load.test.ts`

**Interfaces:**
- Consumes: `daily28: number[]` (índice 27 = hoy), `weekAU: number` (float, suma de `daily7`), `historyDays: number` — todos ya calculados dentro de `loadMetrics`.
- Produces:
  - `export type LoadBandStatus = "descarga" | "sostenible" | "elevada" | "alta";`
  - `export type LoadBand = { weekAU: number; low: number; high: number; overreach: number; status: LoadBandStatus; provisional: boolean };`
  - `LoadMetrics` gana `band: LoadBand | null;`
  - `computeLoadBand(daily28: number[], weekAU: number, historyDays: number): LoadBand | null`

- [ ] **Step 1: Escribir los tests que fallan**

Añadir al final de `frontend/lib/load.test.ts`:

```ts
describe("loadMetrics().band", () => {
  beforeEach(() => localStorage.clear());

  test("historial < 14 días => band null", () => {
    const now = Date.now();
    const sessions = Array.from({ length: 12 }, (_, i) => ({ ts: now - i * DAY, load: 100 }));
    seedActivities(sessions);
    expect(loadMetrics().band).toBeNull();
  });

  test("28 días de carga estable, semana en rango => sostenible y no provisional", () => {
    const now = Date.now();
    const sessions = Array.from({ length: 28 }, (_, i) => ({ ts: now - i * DAY, load: 100 }));
    seedActivities(sessions);
    const b = loadMetrics().band!;
    expect(b.status).toBe("sostenible");
    expect(b.provisional).toBe(false);
  });

  test("baseline estable con suelo de anchura: sigma=0 => banda ±10% de mu (630..770)", () => {
    const now = Date.now();
    const sessions = Array.from({ length: 28 }, (_, i) => ({ ts: now - i * DAY, load: 100 }));
    seedActivities(sessions);
    const b = loadMetrics().band!;
    // mu=700 (7×100), sigma=0 => sigmaEff=70 => low=630, high=770
    expect(b.low).toBe(630);
    expect(b.high).toBe(770);
    expect(b.overreach).toBe(840);
  });

  test("pico fuerte esta semana sobre baseline estable => alta", () => {
    const now = Date.now();
    // días 7..27 atrás a 100 (baseline); últimos 7 días (0..6) a 300 (pico)
    const sessions = Array.from({ length: 28 }, (_, i) => ({ ts: now - i * DAY, load: i <= 6 ? 300 : 100 }));
    seedActivities(sessions);
    expect(loadMetrics().band!.status).toBe("alta");
  });

  test("semana muy suave sobre baseline estable => descarga", () => {
    const now = Date.now();
    const sessions = Array.from({ length: 28 }, (_, i) => ({ ts: now - i * DAY, load: i <= 6 ? 20 : 100 }));
    seedActivities(sessions);
    expect(loadMetrics().band!.status).toBe("descarga");
  });

  test("20 días de historial => banda provisional", () => {
    const now = Date.now();
    const sessions = Array.from({ length: 20 }, (_, i) => ({ ts: now - i * DAY, load: 100 }));
    seedActivities(sessions);
    expect(loadMetrics().band!.provisional).toBe(true);
  });
});
```

- [ ] **Step 2: Ejecutar los tests y ver que fallan**

Run: `cd frontend && pnpm test`
Expected: FAIL — `band` no existe en el tipo/objeto (error de TypeScript o `band` undefined → `.status`/`.low` sobre null).

- [ ] **Step 3: Añadir los tipos y el campo `band`**

En `frontend/lib/load.ts`, tras la definición de `LoadPoint` (línea 6):

```ts
export type LoadBandStatus = "descarga" | "sostenible" | "elevada" | "alta";

export type LoadBand = {
  weekAU: number;    // carga de la semana en curso (marcador)
  low: number;       // límite inferior del rango habitual (μ − σ_eff)
  high: number;      // límite superior del rango habitual (μ + σ_eff)
  overreach: number; // umbral de carga alta (μ + 2σ_eff)
  status: LoadBandStatus;
  provisional: boolean; // 14..27 días de historial: el rango aún se calibra
};
```

En el tipo `LoadMetrics` (líneas 48–57), añadir el campo antes de `historyDays`:

```ts
  band: LoadBand | null; // carga semanal vs rango propio (P0.1); null si historial < 14 días
```

- [ ] **Step 4: Implementar `computeLoadBand`**

En `frontend/lib/load.ts`, justo antes de `export function loadMetrics()` (línea 59):

```ts
/**
 * Banda de carga tipo Strava: sitúa la carga de la semana en curso dentro del
 * rango de las semanas previas del propio atleta (no umbrales fijos). Devuelve
 * null si no hay al menos una semana de baseline antes de la semana actual.
 */
export function computeLoadBand(
  daily28: number[],
  weekAU: number,
  historyDays: number,
): LoadBand | null {
  if (historyDays < 14) return null;

  // Ventanas móviles de 7 días que terminan ANTES de la semana en curso (índices
  // 21..27), acotadas al historial real para no incluir días sin datos.
  const d0 = 28 - historyDays;          // primer índice con datos reales
  const firstEnd = Math.max(6, d0 + 6); // primer día-fin con ventana completa dentro del historial
  const samples: number[] = [];
  for (let e = firstEnd; e <= 20; e++) {
    let s = 0;
    for (let i = e - 6; i <= e; i++) s += daily28[i];
    samples.push(s);
  }
  if (samples.length === 0) return null;

  const mu = samples.reduce((a, b) => a + b, 0) / samples.length;
  if (mu <= 0) return null; // baseline sin carga: nada que comparar

  const variance = samples.reduce((a, v) => a + (v - mu) ** 2, 0) / samples.length;
  const sigma = Math.sqrt(variance);
  const sigmaEff = Math.max(sigma, 0.1 * mu); // suelo de anchura (estabilidad visual, no umbral de seguridad)

  const low = mu - sigmaEff;
  const high = mu + sigmaEff;
  const overreach = mu + 2 * sigmaEff;

  let status: LoadBandStatus;
  if (weekAU < low) status = "descarga";
  else if (weekAU <= high) status = "sostenible";
  else if (weekAU <= overreach) status = "elevada";
  else status = "alta";

  return {
    weekAU: Math.round(weekAU),
    low: Math.max(0, Math.round(low)),
    high: Math.round(high),
    overreach: Math.round(overreach),
    status,
    provisional: historyDays < 28,
  };
}
```

- [ ] **Step 5: Cablear `band` en el return de `loadMetrics`**

En `frontend/lib/load.ts`, sustituir el `return` (línea 102) por:

```ts
  const band = computeLoadBand(daily28, weekAU, historyDays);

  return { weekAU: Math.round(weekAU), daily7, acwr, provisional, monotonia, tension, sinVariacion, historyDays, band };
```

- [ ] **Step 6: Añadir `band: null` en `fetchServerMetrics`**

En `frontend/lib/activities.ts`, en el objeto devuelto (líneas 330–339), añadir tras `historyDays: d.history_days,`:

```ts
      band: null, // el motor de servidor aún no expone la banda (parity: follow-up); la UI usa la local
```

- [ ] **Step 7: Ejecutar los tests y ver que pasan**

Run: `cd frontend && pnpm test`
Expected: PASS — los 6 casos de `loadMetrics().band` + los tests existentes (que no referencian `band`) siguen verdes.

- [ ] **Step 8: Commit**

```bash
cd frontend && git add lib/load.ts lib/load.test.ts lib/activities.ts
git commit -m "feat(carga): banda de carga personalizada en el motor (P0.1)

Añade computeLoadBand y el campo band a LoadMetrics: sitúa la carga
semanal dentro del rango propio del atleta (media±desviación de las
ventanas móviles de 7 días de las 3 semanas previas), con estados
descarga/sostenible/elevada/alta. El ACWR se conserva sin cambios."
```

---

## Task 2: Vista de Carga — tarjeta de banda en lugar de ACWR

**Files:**
- Modify: `frontend/app/(shell)/training/load/page.tsx`
  - import (línea 7): añadir tipos `LoadBand`, `LoadBandStatus`
  - `INFO` (líneas 11–17): quitar clave `acwr`, añadir `banda`
  - `useEffect` (líneas 50–55): preservar banda local al llegar métricas de servidor
  - helper `acwrPct` (línea 61): eliminar (ya no se usa)
  - sección `{/* ACWR */}` (líneas 98–136): sustituir por la tarjeta de banda
  - añadir a nivel de módulo el mapa `BAND_META` y el helper `bandGeometry`

**Interfaces:**
- Consumes de Task 1: `LoadMetrics.band: LoadBand | null` con campos `{ weekAU, low, high, overreach, status, provisional }` y `status ∈ {"descarga","sostenible","elevada","alta"}`.
- Produces: solo cambios de UI; no exporta nada nuevo.

> Esta tarea no lleva test unitario (es UI). Se verifica con typecheck + build + inspección visual (Step 6–7).

- [ ] **Step 1: Ajustar imports y el diccionario INFO**

En `frontend/app/(shell)/training/load/page.tsx`:

Cambiar la línea 7 (la vista solo necesita `loadMetrics` y los tipos `LoadMetrics`/`LoadBand`/`LoadBandStatus`; NO importa `computeLoadBand`):

```ts
import { loadMetrics, type LoadMetrics, type LoadBand, type LoadBandStatus } from "@/lib/load";
```

En el objeto `INFO` (líneas 11–17), eliminar la entrada `acwr:` y añadir:

```ts
  banda: "Tu carga de esta semana comparada con tu propio rango de las últimas 3 semanas: verde = dentro de tu rango habitual; ámbar/rojo = por encima (vigila la recuperación); azul = por debajo (semana de descarga). Se calibra hasta acumular 4 semanas de historial.",
```

- [ ] **Step 2: Añadir `BAND_META` y `bandGeometry` a nivel de módulo**

En `frontend/app/(shell)/training/load/page.tsx`, tras el objeto `INFO` (antes de `function dayLabels`):

```ts
const BAND_META: Record<LoadBandStatus, { label: string; color: string; hint: string }> = {
  descarga: { label: "Descarga", color: "var(--color-neon)", hint: "Por debajo de tu rango: semana de recuperación." },
  sostenible: { label: "Sostenible", color: "var(--color-good)", hint: "Dentro de tu rango habitual: carga sostenible." },
  elevada: { label: "Elevada", color: "var(--color-warn)", hint: "Por encima de tu rango: vigila la recuperación." },
  alta: { label: "Alta", color: "var(--color-bad)", hint: "Muy por encima de tu rango: prioriza recuperar." },
};

// Geometría de la banda: escala 0..(overreach + σ_eff) para que el marcador no se salga.
function bandGeometry(b: LoadBand) {
  const sigmaEff = (b.high - b.low) / 2;
  const axisMax = b.overreach + sigmaEff || 1;
  const pct = (v: number) => Math.max(0, Math.min(100, (v / axisMax) * 100));
  return { pct };
}
```

- [ ] **Step 3: Preservar la banda local al llegar métricas de servidor**

Sustituir el `useEffect` (líneas 50–55) por:

```ts
  useEffect(() => {
    let alive = true;
    const local = loadMetrics();
    setMetrics(local); // pintura inmediata con lo local
    // El servidor manda en las métricas agregadas, pero aún no calcula la banda:
    // conservamos la banda local hasta que el backend la provea (parity: follow-up).
    fetchServerMetrics().then((s) => {
      if (alive && s) setMetrics({ ...s, band: s.band ?? local.band });
    });
    return () => { alive = false; };
  }, []);
```

- [ ] **Step 4: Eliminar el helper `acwrPct`**

Borrar la línea 61:

```ts
  const acwrPct = (v: number) => Math.max(0, Math.min(100, ((v - 0.5) / (1.8 - 0.5)) * 100));
```

- [ ] **Step 5: Sustituir la sección ACWR por la tarjeta de banda**

Reemplazar todo el bloque `{/* ACWR */}` (líneas 98–136, la `<section>` completa) por:

```tsx
      {/* Carga vs tu rango */}
      <section className="glass mt-4 p-4">
        <div className="flex items-center justify-between">
          <div className="flex items-center gap-1.5">
            <span className="t-label text-ink">Carga vs tu rango</span>
            <Info k="banda" open={open} setOpen={setOpen} />
          </div>
          <span className="t-label text-muted">{m?.weekAU ?? 0} AU</span>
        </div>
        {m?.band && m.weekAU > 0 ? (
          <>
            <div className="mt-2 flex items-center gap-3">
              <span className="h-3.5 w-3.5 shrink-0 rounded-full" style={{ background: BAND_META[m.band.status].color, boxShadow: `0 0 12px ${BAND_META[m.band.status].color}` }} />
              <p className="t-display text-2xl" style={{ color: BAND_META[m.band.status].color }}>{BAND_META[m.band.status].label}</p>
            </div>
            {m.band.provisional && (
              <p className="t-body mt-1 text-[11px] text-muted">Rango calibrándose: {m.historyDays}/28 días de historial.</p>
            )}
            {(() => {
              const b = m.band;
              const { pct } = bandGeometry(b);
              return (
                <>
                  <div className="relative mt-3 h-2.5 w-full overflow-hidden rounded-full bg-[rgba(150,190,255,0.12)]">
                    <div className="absolute inset-y-0" style={{ left: 0, width: `${pct(b.low)}%`, background: "rgba(69,233,255,0.30)" }} />
                    <div className="absolute inset-y-0" style={{ left: `${pct(b.low)}%`, width: `${pct(b.high) - pct(b.low)}%`, background: "rgba(67,232,160,0.35)" }} />
                    <div className="absolute inset-y-0" style={{ left: `${pct(b.high)}%`, width: `${pct(b.overreach) - pct(b.high)}%`, background: "rgba(255,210,90,0.32)" }} />
                    <div className="absolute inset-y-0" style={{ left: `${pct(b.overreach)}%`, right: 0, background: "rgba(255,93,128,0.32)" }} />
                    <div className="glow absolute top-1/2 h-4 w-4 -translate-x-1/2 -translate-y-1/2 rounded-full bg-neon" style={{ left: `${pct(b.weekAU)}%` }} />
                  </div>
                  <div className="mt-1.5 flex justify-between text-[10px] text-muted"><span>descarga</span><span className="text-good">tu rango</span><span>alta</span></div>
                </>
              );
            })()}
            <p className="t-body mt-2 text-xs text-muted">{BAND_META[m.band.status].hint}</p>
          </>
        ) : m && m.weekAU === 0 && m.band ? (
          <p className="t-body mt-2 text-xs text-muted">Sin sesiones esta semana: registra un entreno y verás dónde cae respecto a tu rango.</p>
        ) : (
          <p className="t-body mt-2 text-xs text-muted">
            {m && m.historyDays > 0
              ? `Se activa con ~2 semanas de sesiones con RPE (llevas ${m.historyDays} ${m.historyDays === 1 ? "día" : "días"}).`
              : "Registra tus entrenos con RPE (Deportes, MMA o Gimnasio) y tu banda de carga se calculará sola."}
          </p>
        )}
        <Tip k="banda" open={open} />
      </section>
```

- [ ] **Step 6: Typecheck, lint y build**

Run: `cd frontend && pnpm lint && pnpm build`
Expected: sin errores de TypeScript ni ESLint. Confirma que no queda ninguna referencia a `acwrPct` ni a la clave `INFO.acwr`.

- [ ] **Step 7: Verificación visual**

Run: `cd frontend && pnpm dev` y abrir `/training/load`.
Comprobar: (a) sin historial → mensaje "Registra tus entrenos…"; (b) con sesiones y ≥14 días → tarjeta "Carga vs tu rango" con etiqueta de estado coloreada, banda azul/verde/ámbar/rojo y marcador; (c) ya no aparece el número ACWR ni "zona óptima 0.8–1.3" en esta vista.

- [ ] **Step 8: Commit**

```bash
cd frontend && git add "app/(shell)/training/load/page.tsx"
git commit -m "feat(carga): banda de rango con semáforo en la vista de Carga (P0.1)

Sustituye la tarjeta de ACWR (número + zona fija 0.8-1.3, refutada por
la auditoría v2) por la banda de carga personalizada: estado coloreado,
banda descarga/rango/alta y marcador de la semana. El ACWR se mantiene
en el motor pero deja de mostrarse aquí. La banda local se conserva
cuando las métricas de servidor no la traen."
```

---

## Follow-up (fuera de alcance de P0.1)

Estas superficies siguen mostrando el ACWR crudo con la zona 0.8–1.3 y quedan para una decisión posterior (posible P0.1b), no se tocan aquí:

- `app/(shell)/training/page.tsx` — mini-tarjeta resumen con ACWR (líneas ~55, 68).
- `app/(shell)/dashboard/page.tsx` — mini-tarjeta resumen con ACWR (líneas ~470, 483).
- `app/(shell)/coach/page.tsx` — el coach usa ACWR para recomendaciones, briefing y respuestas de chat, y lo envía al backend de IA (líneas ~36, 67, 163, 190, 302, 342). Aquí el ACWR es contexto interno legítimo (el diseño lo conserva en el motor), pero el texto "Zona segura: 0.8–1.3" (línea 191) y los badges visibles al usuario merecen revisarse en su propio ciclo.

## Backend parity (follow-up)

Exponer `band` desde el motor de carga de servidor (Django, PR #21) en `/api/proxy/activities/metrics` y mapearlo en `fetchServerMetrics`, para que la banda refleje también las sesiones registradas en otros dispositivos. Hasta entonces la UI usa la banda local (Task 2, Step 3).
